# Independent rerun

These CSV and JSON files are a separately preserved complete run of
`compare_intermediate_routes.py`. After preservation, the script was run again
from a clean Python process. The regenerated primary CSV and JSON matched these
files byte for byte.
